version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: wd_db
    restart: always
    environment:
      POSTGRES_DB: "${DATABASE_NAME}"
      PGUSER: "${DATABASE_USER}"
      POSTGRES_PASSWORD: "${DATABASE_PASSWORD}"
      
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - "${DATABASE_OUTER_PORT}:${DATABASE_INNER_PORT}"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready", "-U", "${DATABASE_USER}", "-d", "${DATABASE_NAME}", ]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: wd_app
    restart: always
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      ngrok:
        condition: service_started
    environment:
      - WEB_APP_URL=${WEB_APP_URL}
      - BOT_TOKEN=${BOT_TOKEN}
      - BOT_OWNER=${BOT_OWNER}
      - BOT_THROTTLE=${BOT_THROTTLE}
      - BOT_THROTTLE_CACHE_MAXSIZE=${BOT_THROTTLE_CACHE_MAXSIZE}
      - DATABASE_NAME=${DATABASE_NAME}
      - DATABASE_USER=${DATABASE_USER}
      - DATABASE_PASSWORD=${DATABASE_PASSWORD}
      - DATABASE_HOST=${DATABASE_HOST}
      - DATABASE_OUTER_PORT=${DATABASE_OUTER_PORT}
      - DATABASE_INNER_PORT=${DATABASE_INNER_PORT}
      - DATABASE_POOL_SIZE=${DATABASE_POOL_SIZE}
      - REDIS_OUTER_PORT=${REDIS_OUTER_PORT}
      - REDIS_INNER_PORT=${REDIS_INNER_PORT}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_DB=${REDIS_DB}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
      - NGROK_DOMAIN=${NGROK_DOMAIN}
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY}
      - DJANGO_HOST=${DJANGO_HOST}
      - DJANGO_PORT=${DJANGO_PORT}
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: ["python", "main.py"]
  
  web_app:
    build:
      dockerfile: web_app/Dockerfile
      context: .
    volumes:
      - .:/web_app
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DJANGO_SETTINGS_MODULE=web_app.app.settings
      - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY}
      - DJANGO_HOST=${DJANGO_HOST}
      - DJANGO_PORT=${DJANGO_PORT}
      - DATABASE_NAME=${DATABASE_NAME}
      - DATABASE_USER=${DATABASE_USER}
      - DATABASE_PASSWORD=${DATABASE_PASSWORD}
      - DATABASE_HOST=${DATABASE_HOST}
      - DATABASE_OUTER_PORT=${DATABASE_OUTER_PORT}
      - DATABASE_INNER_PORT=${DATABASE_INNER_PORT}
      - DATABASE_POOL_SIZE=${DATABASE_POOL_SIZE}
      - BOT_TOKEN=${BOT_TOKEN}
      - BOT_OWNER=${BOT_OWNER}
      - BOT_THROTTLE=${BOT_THROTTLE}
      - BOT_THROTTLE_CACHE_MAXSIZE=${BOT_THROTTLE_CACHE_MAXSIZE}
    ports:
      - "80:${DJANGO_PORT}"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:${DJANGO_PORT}/health/')"]
      interval: 2m
      timeout: 5s
      retries: 5
      start_period: 10s
    command: ["uvicorn", "web_app.app.asgi:application", "--host", "${DJANGO_HOST}", "--port", "${DJANGO_PORT}"]
  
  redis:
    image: redis:latest
    container_name: wd_redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes --appendfsync everysec --bind 0.0.0.0
    volumes:
      - redis_data:/data
    ports:
      - "${REDIS_OUTER_PORT}:${REDIS_INNER_PORT}"
    healthcheck:
      test: [ "CMD", "redis-cli", "--raw", "-a", "${REDIS_PASSWORD}", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
  
  ngrok:
    image: ngrok/ngrok:latest
    restart: unless-stopped
    command:
      - "http"
      - "web_app:8080"
      - "--url=${NGROK_DOMAIN}"
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    ports:
      - "4040:4040"
    depends_on:
      web_app:
        condition: service_healthy
    
volumes:
  postgres_data:
    driver: local
  test_postgres_data:
    driver: local
  redis_data:
    driver: local